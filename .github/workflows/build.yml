name: Build and Deploy

on:
  push:
    branches:
      - main

env:
  WORKING_DIRECTORY: src
  WEBAPP_NAME: DotNetFrameworkActions.sln     # set this to your application's name
  WEBAPP_PACKAGE_PATH: 'published'      # set this to the path to your web app project, defaults to the repository root

jobs:
  build:
    name: Build and deploy
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup MSBuild path
      uses: microsoft/setup-msbuild@v2

    - name: Setup NuGet
      uses: nuget/setup-nuget@v2
    
    - name: Restore NuGet packages
      run: nuget restore ${{ env.WEBAPP_NAME }}
      working-directory: ${{ env.WORKING_DIRECTORY }}

    - name: Build
      run: msbuild ${{ env.WEBAPP_NAME }} /p:Configuration=Debug
      working-directory: ${{ env.WORKING_DIRECTORY }}

    # - name: Run tests
    #   uses: microsoft/vstest-action@v1.0.0
    #   with:
    #     testSelector: 'testAssemblies'
    #     testAssemblyVer2: 'DotNetFrameworkActions.UnitTest\bin\Debug\DotNetFrameworkActions.UnitTest.dll'
    #     working-directory: ${{ env.WORKING_DIRECTORY }}

    - name: Execute unit tests
      run: dotnet test

    - name: Publish to folder
      run: msbuild DotNetFrameworkActions.WebApp\DotNetFrameworkActions.WebApp.csproj /p:Configuration=Release /p:DeployOnBuild=true /t:WebPublish /p:WebPublishMethod=FileSystem /p:publishUrl=${{ env.WEBAPP_PACKAGE_PATH }} /p:PackageAsSingleFile=false
      working-directory: ${{ env.WORKING_DIRECTORY }}

    - name: Publish artifacts
      uses: actions/upload-artifact@v4
      with:
        working-directory: ${{ env.WORKING_DIRECTORY }}
        name: webapp
        path: ${{ env.WEBAPP_PACKAGE_PATH }}